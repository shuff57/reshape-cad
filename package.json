{
  "name": "reshape-cad-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @shuff57/reshape-sketch && npm run build -w @shuff57/reshape-script && npm run build -w @shuff57/reshape-kernel && npm run build -w @shuff57/reshape-studio",
    "test": "node scripts/check-record.mjs bench/record.json && npm test --workspaces --if-present && npm run test:occt",
    "test:occt": "node scripts/occt-modeldoc-gate.mjs"
  },
  "devDependencies": {
    "replicad-opencascadejs": "1.1.0"
  }
}
