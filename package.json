{
  "name": "reshape-cad-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "node scripts/check-record.mjs bench/record.json && npm test --workspaces --if-present"
  }
}
